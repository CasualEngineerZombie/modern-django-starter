"""
Development settings for {{ project_name }} project.
"""

from .base import *

DEBUG = True

{% if not config.api_only %}
INSTALLED_APPS += [
    'debug_toolbar',
    'django_extensions',
]

MIDDLEWARE = [
    'debug_toolbar.middleware.DebugToolbarMiddleware',
] + MIDDLEWARE
{% endif %}

# Database settings are inherited from base.py so Docker and local
# development use the same environment-driven configuration.

INTERNAL_IPS = [
    '127.0.0.1',
    'localhost',
]

# Console email backend for development.
MAILERS = {
    'default': {
        'BACKEND': 'django.core.mail.backends.console.EmailBackend',
    },
}

{% if not config.api_only %}
STATICFILES_DIRS = [
    BASE_DIR / 'static',
]
{% endif %}

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.locmem.LocMemCache',
    }
}

LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'console': {
            'class': 'logging.StreamHandler',
        },
    },
    'root': {
        'handlers': ['console'],
        'level': 'INFO',
    },
    'loggers': {
        'django': {
            'handlers': ['console'],
            'level': 'INFO',
            'propagate': False,
        },
    },
}
